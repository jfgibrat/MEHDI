# 1) To compile the program :
> cd SOURCE
> make or make "OPTIM= -DKLUDGE -O3"

# 2) First define the following environment variable (see file SOURCE/alloc.h)
> cd ../
> export GENERATE_3GEN_READS_DIR=$PWD/SOURCE

# 3) Run the program
> SOURCE/generate_3gen_reads [program options] (if no specified options, print program usage)
